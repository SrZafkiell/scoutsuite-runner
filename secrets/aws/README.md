# AWS credentials folder

Local-only credential files go here.

Expected files:

```text
credentials
config
```

Create them with:

```powershell
.\scripts\init-aws-creds.ps1
```

These files are ignored by Git.
